import Tries from './tries.container';
import saga from './tries.sagas';
import reducer from './tries.reducer';

export const triesSaga = saga;
export const triesReducer = reducer;

export default Tries;
